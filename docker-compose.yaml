services:
    dev:
        container_name: dev
        build:
            context: .
            dockerfile: Dockerfile
            target: dev
        volumes:
            - .:/app
            - /app/node_modules
        ports:
            - "8000:8000"

    pdf:
        container_name: pdf
        build:
            context: .
            dockerfile: Dockerfile
            target: base
        volumes:
            - .:/app
            - /app/node_modules
            - /app/pdf/node_modules
        working_dir: /app/pdf
